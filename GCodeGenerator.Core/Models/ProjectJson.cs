#nullable enable
using System.Text.Json;

namespace GCodeGenerator.Models
{
    /// <summary>
    /// Настройки сериализации формата проекта — единственный экземпляр
    /// на файл, клон операции и слепок генерации.
    ///
    /// Файл проекта, рабочая копия операции в диалоге и слепок документа
    /// для фоновой генерации обязаны быть одним и тем же представлением:
    /// клон «по определению совпадает с составом сохраняемых данных» ровно
    /// до тех пор, пока настройки сериализации общие. Прежде каждый из
    /// четырёх участников заводил собственный экземпляр, и их одинаковость
    /// держалась на совпадении, а не на конструкции: первый же конвертер,
    /// добавленный в одну из копий, молча развёл бы файл и его копии.
    /// </summary>
    public static class ProjectJson
    {
        /// <summary>Общие настройки сериализации данных проекта.</summary>
        public static readonly JsonSerializerOptions Options = new JsonSerializerOptions();
    }
}
