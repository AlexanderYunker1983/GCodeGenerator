<Window x:Class="GCodeGenerator.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:GCodeGenerator.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:SettingsViewModel}"
        Title="{DataBinding '$i18n.GCodeSettingsTitle'}"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <CheckBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                  Content="{DataBinding '$i18n.UseLineNumbers'}"
                  IsChecked="{Binding UseLineNumbers}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,8,8,0"
                   Text="{DataBinding '$i18n.LineNumberStart'}" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,8,0,0" Width="80"
                 Text="{Binding LineNumberStart, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,8,8,0"
                   Text="{DataBinding '$i18n.LineNumberStep'}" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="0,8,0,0" Width="80"
                 Text="{Binding LineNumberStep, UpdateSourceTrigger=PropertyChanged}"/>

        <CheckBox Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,8,0,0"
                  Content="{DataBinding '$i18n.GenerateComments'}"
                  IsChecked="{Binding UseComments}"/>

        <CheckBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,8,0,0"
                  Content="{DataBinding '$i18n.AllowArcs'}"
                  IsChecked="{Binding AllowArcs}"/>
    </Grid>
</Window>


