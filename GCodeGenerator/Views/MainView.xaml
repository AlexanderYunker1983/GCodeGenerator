<Window x:Class="GCodeGenerator.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:GCodeGenerator.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:MainViewModel}"
        Title="{DataBinding '$i18n.MainTitle'}" Height="600" Width="900">
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="{DataBinding '$i18n.AddDrillPoints'}"
                    Command="{Binding AddDrillPointsCommand}" Margin="0,0,8,0"/>
            <Button Content="{DataBinding '$i18n.GenerateGCode'}"
                    Command="{Binding GenerateGCodeCommand}" Margin="0,0,8,0"/>
            <Button Content="{DataBinding '$i18n.Settings'}"
                    Command="{Binding OpenSettingsCommand}"/>
        </StackPanel>

        <GroupBox Header="{DataBinding '$i18n.OperationsHeader'}"
                  Grid.Row="1" Grid.Column="0" Margin="0,0,8,0">
            <ListBox ItemsSource="{Binding Operations}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Path=., ConverterCulture='en-US'}"
                                       TextTrimming="CharacterEllipsis"
                                       FontSize="11"
                                       Foreground="Gray"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <GroupBox Header="{DataBinding '$i18n.GCodePreviewHeader'}"
                  Grid.Row="1" Grid.Column="1">
            <TextBox Text="{Binding GCodePreview}" FontFamily="Consolas"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     IsReadOnly="True"/>
        </GroupBox>
    </Grid>
</Window>